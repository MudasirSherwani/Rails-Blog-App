<% if (post.comments.empty?) %>
  <p>No comment yet.</p>
<% else %>
  <% post.comments.each do |comment| %>
    <ul>
      <li><%= comment.users.name %>: <%= comment.text %></li>
    </ul>
  <% end %>
<% end %>